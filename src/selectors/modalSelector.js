export const selectIsPostModalOpen = (state) => state.postModal.isModalOpen;

export const selectIsInlineModalOpen = (state) => state.inlineModal.isInlineModalOpen;
export const selectInlineModalTopPosition = (state) => state.inlineModal.topPos;
export const selectInlineModalLeftPosition = (state) => state.inlineModal.leftPos;
